version: '3.8'

services:
  web:
    build: .
    container_name: tcc_django_app
    restart: always
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - .:/app
    environment:
      - DJANGO_DEBUG=False # Desativa modo de depuração para produção
      - DJANGO_SECRET_KEY=substitua_por_uma_chave_segura_em_producao
